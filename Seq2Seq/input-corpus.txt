# Py2Pj
sd = 0, s += int(x)<tab>sをxの整数値だけ増加させる, return s, for i in range(l):, c += 1, if n <= 20:, m = 0, if m < N:, while n != 1:, m = n

# Pj2Py
sdを0とする, sをxの整数値だけ増加させる, sが関数出力となる, {{0からl未満までの数列}}を先頭から順にiとして、以下を繰り返す, cを1だけ増加させる, もしnが20以下のとき、, mを0とする, もしmがNより小さいとき、, もしnが1と等しくないとき、以下を繰り返す, mをnとする

# 入力にコピーして貼り付ける